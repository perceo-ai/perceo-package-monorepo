# Perceo Temporal Worker Deployment Configuration
# Copy this to .env.deploy and fill in your values
# NEVER commit .env.deploy to git!

# ============================================================================
# Docker/GCP Configuration (for worker deployment)
# ============================================================================

# Docker image name
IMAGE=ghcr.io/perceo-ai/perceo-package-monorepo/temporal-worker:latest
GCP_PROJECT=perceo
GCP_REGION=us-west1
BUILD_LOCALLY=1
SKIP_PUSH=0

# ============================================================================
# Temporal Configuration (Required for Worker)
# ============================================================================

# Temporal Cloud address (or self-hosted address)
PERCEO_TEMPORAL_ADDRESS=us-west1.gcp.api.temporal.io:7233

# Temporal API key (JWT token from Temporal Cloud)
PERCEO_TEMPORAL_API_KEY=eyJhbGciOiJFUzI1NiIsICJraWQiOiIuLi4ifQ...

# Temporal namespace (format: namespace.account-id)
PERCEO_TEMPORAL_NAMESPACE=your-namespace.account-id

# Temporal task queue name
PERCEO_TEMPORAL_TASK_QUEUE=perceo-task-queue

# ============================================================================
# Supabase Configuration (Required for Worker to write to database)
# ============================================================================

# Your Supabase project URL
PERCEO_SUPABASE_URL=https://your-project.supabase.co

# Supabase publishable/anon key (for CLI users)
PERCEO_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Supabase service role key (from project settings → API)
# ⚠️  KEEP THIS SECRET - Never expose to clients!
PERCEO_SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Supabase database password (for direct DB access if needed)
PERCEO_SUPABASE_PASSWORD=your-db-password

# ============================================================================
# Worker API Configuration (for HTTP endpoints)
# ============================================================================

# API key for worker HTTP endpoints (optional but recommended)
PERCEO_WORKER_API_KEY=your-secure-random-api-key

# ============================================================================
# LLM API Configuration (At least one required)
# ============================================================================

# OpenRouter API key (recommended)
PERCEO_OPEN_ROUTER_API_KEY=sk-or-v1-...

# OR Anthropic API key (alternative)
# PERCEO_ANTHROPIC_API_KEY=sk-ant-...

# ============================================================================
# Optional: Neo4j Flow Graph
# ============================================================================

# PERCEO_NEO4J_ENDPOINT=neo4j+s://your-db.databases.neo4j.io:7687
# PERCEO_NEO4J_USERNAME=neo4j
# PERCEO_NEO4J_PASSWORD=your-password
# PERCEO_NEO4J_DATABASE=perceo
