{
	"name": "flows_from_graph",
	"strict": true,
	"schema": {
		"type": "object",
		"properties": {
			"flows": {
				"type": "array",
				"items": {
					"type": "object",
					"properties": {
						"name": { "type": "string", "description": "Flow name, meaningful journey (e.g. 'Complete checkout', 'Sign in and reach dashboard')" },
						"description": { "type": "string", "description": "What this flow accomplishes" },
						"pages": {
							"type": "array",
							"items": { "type": "string" },
							"description": "Route paths this flow uses, typically 2-5 (e.g. ['/login', '/dashboard'])"
						},
						"connectedFlowIds": {
							"type": "array",
							"items": { "type": "string" },
							"description": "Names of flows this flow connects to"
						}
					},
					"required": ["name", "description", "pages"],
					"additionalProperties": false
				}
			}
		},
		"required": ["flows"],
		"additionalProperties": false
	}
}
